<Window x:Class="AiS.Views.ApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:AiS.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d" 
        Title="{Binding ApplicationName}" 
        MinWidth="600" Width="600"
        MinHeight="400" Height="400"
        DataContext="{Binding}">
    <Window.InputBindings>
        <KeyBinding Key="F4" Modifiers="Alt" Command="{Binding AltF4Command}"/>
        <KeyBinding Key="F5" Command="{Binding CurrentWindow.RefreshCommand}"/>
    </Window.InputBindings>
    <Grid>
        <views:MenuView x:Name="menuControl" VerticalAlignment="Top" Width="560" Height="25" Margin="0,1,0,0"/>
        <ContentControl x:Name="contentWindow" Content="{Binding CurrentWindow}" Margin="0,27,0,0"/>
    </Grid>
</Window>
