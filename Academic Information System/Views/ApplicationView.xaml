<Window x:Class="AiS.Views.ApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:AiS.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d" 
        Title="{Binding ApplicationName}" 
        MinWidth="600" Width="600"
        MinHeight="400" Height="400"
        DataContext="{Binding}">
    <Grid>
        <ContentControl x:Name="contentWindow" Margin="0,35,0,0" Content="{Binding CurrentWindow}"/>
        <views:MenuView VerticalAlignment="Top" Width="560" />
    </Grid>
</Window>
